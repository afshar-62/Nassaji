# گزارش جامع ممیزی فنی و نقشه‌برداری معماری پلتفرم تاروپود (TAROPOD)
**سند ارزیابی وضعیت موجود (AS-IS Architectural Audit & Technical Mapping)**  
**مرحله:** فقط ممیزی و نقشه‌برداری معماری (بدون بازنویسی یا دستکاری کدهای اجرایی)  
**تاریخ گزارش:** سپتامبر ۲۰۲۶  
**نسخه سند:** 1.0.0-AUDIT  

---

## مقدمه و زمینه بنیادین محصول (Product Context)

تاروپود یک **«پلتفرم و زیرساخت دیجیتال B2B تخصصی برای کل زنجیره ارزش صنعت نساجی، پوشاک و صنایع وابسته»** است. تاروپود صرفاً یک سایت آگهی یا فروشگاه ساده پوشاک نیست. افق میان‌مدت و بلندمدت این زیرساخت باید از موجودیت‌های متنوعی شامل:
* کالاها و منسوجات (Products)
* مواد اولیه و الیاف (Materials)
* تجهیزات و ماشین‌آلات (Equipment)
* خدمات تخصصی و کارمزدی (Services)
* محصولات دیجیتال و الگوها (Digital Products)
* ظرفیت‌های خالی کارگاه‌ها و خطوط (Capacity)
* فرصت‌های شغلی و نیروی انسانی (Jobs)
* نیازمندی‌ها و درخواست‌های خرید (Needs / Demands)
* فرصت‌های سرمایه‌گذاری و مزایده‌ها (Opportunities)
* پروژه‌های پیمانکاری و خطوط مشارکتی (Projects)

پشتیبانی نماید؛ با این قید که هر کنشگر (فرد یا بنگاه) می‌تواند به‌صورت هم‌زمان دارای نقش‌های متعدد باشد (تولیدکننده، تأمین‌کننده، خریدار، کارفرما، کارجو، ارائه‌دهنده خدمت، متخصص، پیمانکار، واردکننده، صادرکننده، توزیع‌کننده و دارنده ظرفیت خالی).

**اصل معماری راهبردی هدف:**
$$\text{User Input} \neq \text{Stored Data} \neq \text{Search Index} \neq \text{AI Derived Data}$$
کاربر باید کمترین حجم اطلاعات کلیدی و باارزش را وارد کند و سیستم باید وظیفه استخراج، استانداردسازی، غنی‌سازی و نمایه‌سازی هوشمند را انجام دهد.

---

## ۱. ارزیابی پشته فناوری (Technology Stack Audit)

| لایه فناوری | فناوری دقیق در سورس‌کد | نقش فعلی در سامانه | مناسب‌بودن ساختاری برای تاروپود | ریسک مقیاس‌پذیری آینده | وضعیت پیشنهادی فعلی |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Frontend** | React 19.0.1 + Vite 6.2.3 + Tailwind CSS v4.1.14 | رندرینگ SPA کلاینت، مودال‌ها، فیدها، تب‌ها، کالیته رنگ | **کاملاً مناسب** (سرعت بالا، کامپوننت‌پذیری، طراحی مدرن) | ندارد (SPA بهینه با Lazy loading پاسخگوی ترافیک بالا است) | **UNCHANGED** |
| **Backend** | Node.js v22 + Express 4.21.2 + TSX 4.21.0 | وب‌سرور REST API، ماژولار مونولیت، میدلورها | **کاملاً مناسب** (ساده، سریع، اکوسیستم غنی جاوااسکریپت) | ندارد (معماری ماژولار قابلیت تبدیل به سرویس‌های مجزا را دارد) | **UNCHANGED** |
| **Database** | PostgreSQL 16 (پایگاه‌داده رابطه‌ای استاندارد) | ذخیره تراکنشی کاربران، کارگاه‌ها، پروفایل‌ها، آگهی‌ها، دسته‌ها | **بسیار عالی و حیاتی** (پشتیبانی از روابط قوی، JSONB، ایندکس‌های کامل) | در مقیاس بسیار بالا نیاز به Read Replicas و پارتیشن‌بندی فید خواهد داشت | **UNCHANGED** |
| **ORM** | Drizzle ORM 0.45.2 + Drizzle Kit 0.31.10 | نگاشت مدل‌ها، تراکنش‌ها، تایپ‌سیفتی SQL، مایگریشن | **بسیار سبک و کارآمد** (بدون سربار سنگین Prisma، SQL خالص) | ندارد (اجرای کوئری‌های خام و بهینه‌سازی بسیار آسان است) | **UNCHANGED** |
| **Authentication** | Firebase Auth (پیکربندی کلاینت) + Bearer Context میدلور | دریافت UID بازیگر و نگاشت به شناسه کاربران سیستم | **مناسب برای آغاز، اما نیازمند تکمیل امنیتی** | وابستگی به احراز هویت خارجی در محیط‌های درون‌کشوری | **UNCHANGED** (افزودن تأیید امضای JWT در مراحل بعد) |
| **Validation** | Zod 4.6.1 + Drizzle schema validation | اعتبارسنجی ورودی‌های آگهی، پروفایل و کوئری‌ها | **بسیار استاندارد و تایپ‌سیف** | ندارد | **UNCHANGED** |
| **AI Integration**| Google GenAI SDK (`@google/genai` 2.4.0) | مشاوره متنی پارچه/ماشین‌آلات با مدل `gemini-2.5-flash` | **مناسب برای پردازش زبان طبیعی** | کنترل هزینه و Rate Limit در مقیاس درخواست‌های انبوه | **UNCHANGED** |
| **File / Media** | ذخیره‌سازی ابری URL + جدول `media` رابطه‌ای | ذخیره URL تصاویر و اتصال چندبه‌چند به آگهی‌ها | **کافی برای فاز جاری**، نیازمند متصل شدن به S3/Object Storage واقعی | ذخیره URL مستقیم بدون پردازش سایز و تبدیل WebP مقیاس‌پذیر نیست | **EXTEND LATER** |
| **Testing** | Node.js Test Runner بومی (`node:test` + TSX) | اجرای تست‌های یکپارچگی برش عمودی (Vertical Slices) | **بسیار سریع و سبک** بدون وابستگی‌های سنگین Jest | در پروژه‌های تیمی نیاز به گزارش‌گیری جامع‌تر دارد | **UNCHANGED** |
| **Build & Dev** | Vite + esbuild + tsx | بیلد کامپوننت‌های فرانت‌اند و باندل سرور Express | **فوق‌العاده سریع** | ندارد | **UNCHANGED** |

---

## ۲. معماری ساختار مخزن (Repository Architecture)

```
taropod/
├── drizzle/                     # اسکریپت‌های SQL و اسنپ‌شات‌های Drizzle ORM
├── public/                      # فایل‌های استاتیک، بسته‌های ZIP، تصاویر آزمایشی
├── server/                      # هسته ماژولار بک‌اند (Modular Monolith)
│   ├── middleware/              # میدلورهای عمومی (auth, context, rateLimit)
│   ├── modules/                 # دامنه‌های تجاری مستقل
│   │   ├── ai/                  # ماژول مشاوره هوش مصنوعی (ai.router.ts)
│   │   ├── businesses/          # ماژول کسب‌وکارها و کارگاه‌ها
│   │   ├── explore/             # موتور جستجو، فیلتر و فید اکسپلور
│   │   ├── listings/            # مدیریت چرخه حیات آگهی‌ها، پیش‌نویس، رسانه و مکان
│   │   ├── locations/           # موقعیت‌های مکانی و توزیع جغرافیایی
│   │   ├── profiles/            # شناسنامه پایدار B2B (فرد / سازمان)، مدارک، امتیاز
│   │   └── taxonomy/            # دسته‌بندی و رسته‌های ۱۱گانه تخصصی نساجی
│   ├── routes/                  # متصل‌کننده اصلی مسیرها (api.ts)
│   ├── seed/                    # سیدهای اولیه (textileMarketSeed.ts)
│   ├── tests/                   # تست‌های یکپارچگی سرور (vertical-slice-*.test.ts)
│   └── types/                   # تایپ‌های اشتراکی سرور (auth.ts, api.ts)
├── src/                         # فرانت‌اند کاربری (React 19 SPA)
│   ├── assets/                  # دارایی‌های بصری و تصاویر دسته‌ها و بیلبوردها
│   ├── components/              # کامپوننت‌های UI (مودال‌ها، فید، پلیر، نقشه، پروفایل)
│   ├── core/                    # کلاینت‌های API و سرویس‌های دامنه کلاینت
│   │   ├── api/                 # سرویس‌های فراخوانی اندپوینت‌های سرور
│   │   └── monetization/        # موتور بررسی بسته انتشار و تسویه (entitlement.service.ts)
│   ├── data/                    # داده‌های پشتیبان کلاینت (locationsData, mockStoriesData)
│   ├── db/                      # تنظیمات و اسکیمای مرکزی دیتابیس (schema.ts, index.ts)
│   ├── design-system/           # توکن‌های بصری
│   ├── types.ts                 # تایپ‌های جامع فرانت‌اند
│   └── utils/                   # ابزارهای کلاینت (themePalette.ts)
└── server.ts                    # نقطه ورود اجرای سرور هیبرید (Express + Vite)
```

**تحلیل قابلیت توسعه ساختار:**  
معماری تفکیک ماژول‌ها در پوشه `server/modules/` با الگوی پورت و آداپتور (Port & Adapter) پیاده‌سازی شده است: هر ماژول دارای فایل‌های `*.router.ts`، `*.service.ts`، `*.repository.port.ts` و `*.repository.postgres.ts` است. این ساختار **بسیار ساختاریافته و ماژولار** بوده و کاملاً توانایی میزبانی و توسعه قابلیت‌های آینده را بدون درهم‌تنیدگی کد دارد.

---

## ۳. وضعیت جاری مدل‌های دامنه (Current Domain Model Status)

| مفهوم دامنه | وضعیت پیاده‌سازی فعلی | فایل پیاده‌سازی و جزئیات دقیق |
| :--- | :--- | :--- |
| **User (کاربر)** | **پیاده‌سازی شده** | جدول `users` در `src/db/schema.ts` (شناسه، UID فایربیس، موبایل، نام، ایمیل، وضعیت فعال). |
| **Person (شخص حقیقی)** | **پیاده‌سازی شده (درون Profile)** | در جدول `profiles` فیلد `profileType = 'PERSON'` همراه با نام و نام خانوادگی، بیوگرافی و تخصص‌ها. |
| **Business (بنگاه / کارگاه)** | **پیاده‌سازی شده** | جدول `businesses` (نام، اسلاگ، شماره ثبت، شماره پروانه، شهر، استان، تأییدیه، مالک). |
| **Profile (شناسنامه)** | **پیاده‌سازی شده** | جدول `profiles` (پوشش‌دهنده فرد و سازمان، سوابق، محصولات و خدمات پایدار، ظرفیت کارگاه، اطلاعات تماس). |
| **Listing / Advertisement** | **پیاده‌سازی شده** | جدول `listings` (عنوان، توضیحات، نوع فعالیت، نوع کالا، نوع قیمت، واحد، حداقل سفارش، شهر، استان، وضعیت انتشار). |
| **Category / Taxonomy** | **پیاده‌سازی شده** | جدول `categories` (اسلاگ، عنوان فارسی و انگلیسی، آیکون، والد `parentId`، ترتیب نمایش). |
| **Location (موقعیت مکانی)** | **پیاده‌سازی شده** | جدول مستقل `locations` و پیوند `listing_locations` و `profile_locations` (مختصات، استان، شهر، شهرک صنعتی). |
| **Media (چندرسانه‌ای)** | **پیاده‌سازی شده** | جدول مستقل `media` و پیوند `listing_media` (آدرس URL، نوع رسانه، کلید ذخیره‌سازی، فیلد کاور). |
| **Activity Type (نوع فعالیت)** | **پیاده‌سازی شده** | ستون `activity_type` در جدول `listings` با مقادیر `'offer'`, `'need'`, `'capacity'`. |
| **Commodity / Product Type**| **پیاده‌سازی شده (ساده)**| ستون `commodity_type` در جدول `listings` با مقادیر پیش‌فرض `'product'`, `'service'`, `'capacity'`, `'material'`, `'machine'`. |
| **Offer (عرضه / ارائه)** | **پیاده‌سازی شده** | در UI به عنوان `ListingIntent = 'OFFER'` و در دیتابیس `activity_type = 'offer'`. |
| **Demand / Need (تقاضا / نیاز)**| **پیاده‌سازی شده** | در UI به عنوان `ListingIntent = 'DEMAND'` و در دیتابیس `activity_type = 'need'`. |
| **Capacity (ظرفیت خالی)** | **پیاده‌سازی شده (اولیه)**| فیلد `capacitySummary` در جدول `profiles` و مقدار `'capacity'` در `activity_type`. |
| **Digital Product (محصول دیجیتال)**| **NOT CURRENTLY IMPLEMENTED** | هیچ فیلد یا مدلی برای فایل الگوهای دانلودی، فرمت‌های CAD/CAM نساجی وجود ندارد. |
| **Job / Opportunity (فرصت شغلی)**| **NOT CURRENTLY IMPLEMENTED** | ساختار مجزایی برای رزومه، شرایط حقوق، بیمه و تخصص‌های شغلی تعریف نشده است. |
| **AI Data / Embeddings** | **NOT CURRENTLY IMPLEMENTED** | هیچ بردار، ایندکس معنایی، فیلد استخراج ویژگی توسط هوش مصنوعی در دیتابیس ذخیره نمی‌شود. |

---

## ۴. ممیزی اسکیمای پایگاه داده فعلی (Database Schema Audit)

| نام جدول در Drizzle | هدف و مسئولیت فعلی | فیلدهای کلیدی | روابط خارجی (Foreign Keys) | وضعیت در نقشه راه آینده |
| :--- | :--- | :--- | :--- | :--- |
| `users` | ذخیره کنشگران و هویت‌های احراز هویت شده | `id`, `uid`, `mobile`, `name`, `email` | ندارد | **KEEP** |
| `businesses` | هویت کارگاه، کارخانه، دفتر تجاری یا فروشگاه | `id`, `name`, `slug`, `city`, `province`, `isVerified` | `createdById -> users.id` | **MODIFY** (همگام‌سازی با Actor/Profile) |
| `business_memberships`| پیوند چندبه‌چند کاربر و کارگاه (نقش مالک، مدیر، اپراتور)| `userId`, `businessId`, `role`, `isDefault` | `userId -> users`, `businessId -> businesses` | **KEEP** |
| `business_profiles` | ویترین اطلاعاتی و تجهیزات کارگاه (مدل اولیه) | `businessId`, `workshopAreaSqm`, `activeMachinesCount` | `businessId -> businesses.id` | **REFACTOR LATER** (ادغام در `profiles`) |
| `categories` | درخت رسته‌ها و دسته‌بندی تخصصی نساجی | `id`, `parentId`, `slug`, `titleFa`, `titleEn`, `icon` | `parentId -> categories.id` (خودارجاعی) | **EXTEND** (افزودن متادیتا و ویژگی‌ها) |
| `media` | موجودیت مستقل دارایی‌های چندرسانه‌ای | `id`, `url`, `mimeType`, `storageKey`, `sizeBytes` | `businessId -> businesses`, `uploadedById -> users` | **EXTEND** |
| `locations` | موجودیت مستقل جغرافیایی و موقعیت واحدها | `id`, `title`, `province`, `city`, `latitude`, `longitude`| ندارد | **KEEP** |
| `listings` | آگهی‌های ثبت‌شده بازارگاه (عرضه، تقاضا، ظرفیت) | `id`, `title`, `activityType`, `commodityType`, `priceAmount`, `status` | `businessId -> businesses`, `categoryId -> categories` | **MODIFY** (افزودن لایه‌های استخراج AI) |
| `listing_media` | جدول پیوند چندبه‌چند آگهی و تصاویر | `listingId`, `mediaId`, `displayOrder`, `isCover` | `listingId -> listings`, `mediaId -> media` | **KEEP** |
| `listing_locations` | جدول پیوند آگهی به موقعیت جغرافیایی | `listingId`, `locationId`, `isPrimary` | `listingId -> listings`, `locationId -> locations` | **KEEP** |
| `profiles` | شناسنامه پایدار B2B کنشگر (فرد یا سازمان) | `id`, `accountId`, `profileType`, `workGroup`, `specialties` | `accountId -> users.id` | **EXTEND** (تبدیل به موجودیت محوری هویت) |
| `profile_credentials`| مدارک، پروانه کسب و گواهی‌های معتبر صنفی | `profileId`, `title`, `fileUrl`, `isOfficiallyVerified` | `profileId -> profiles.id` | **KEEP** |
| `profile_locations` | شعب، کارگاه‌ها، دفاتر و انبارهای یک پروفایل | `profileId`, `unitTitle`, `unitType`, `city`, `latitude` | `profileId -> profiles.id` | **KEEP** |
| `profile_ratings` | امتیازات تعاملی بر پایه تراکنش‌های واقعی | `profileId`, `raterAccountId`, `score`, `interactionType` | `profileId -> profiles`, `raterAccountId -> users` | **KEEP** |
| `profile_audit_events`| لاگ رویدادها و تغییرات امنیتی شناسنامه | `profileId`, `actorId`, `eventType`, `metadata` | `profileId -> profiles`, `actorId -> users` | **KEEP** |
| `publication_packages`| بسته‌های خریداری‌شده تجاری کنشگر برای انتشار | `accountId`, `planId`, `totalCapacity`, `remainingCapacity`| `accountId -> users.id` | **KEEP** |
| `package_entitlement_consumptions`| دفتر کل مصرف سهمیه انتشار به ازای هر آگهی | `packageId`, `listingId`, `consumedByAccountId`, `unitsConsumed`| `packageId -> publicationPackages`, `listingId -> listings` | **KEEP** |
| `listing_payment_transactions`| ثبت تراکنش‌های مالی تکی انتشار آگهی | `listingId`, `payerAccountId`, `amount`, `status` | `listingId -> listings`, `payerAccountId -> users` | **KEEP** |

---

## ۵. ممیزی سیستم تاکسونومی و دسته‌بندی (Taxonomy Audit)

1. **آیا تاکسونومی سلسله‌مراتبی است؟**
   * **بله در دیتابیس و سیدها:** جدول `categories` دارای فیلد `parentId` (خودارجاعی) است و `taxonomyService.getCategoryTree()` درخت دوبخشی را بازمی‌گرداند.
2. **آیا تاکسونومی هاردکد شده است یا مبتنی بر پایگاه داده؟**
   * **مبتنی بر دیتابیس با هماهنگ‌سازی اولیه سید:** دسته‌ها در جدول دیتابیس `categories` ذخیره می‌شوند؛ اما در صورت خالی بودن دیتابیس، از فایل `server/modules/taxonomy/taxonomy.seeds.ts` با ۱۱ رسته ریشه و زیرمجموعه‌ها لود می‌شوند. در فرانت‌اند نیز در کامپوننت `ExploreMarket.tsx` یک آرایه ثابت به نام `TEXTILE_NAV_TAGS` به‌صورت هاردکد وجود دارد که روی متن‌ها فیلتر می‌زند.
3. **آیا دسته‌ها می‌توانند عمق دلخواه (Arbitrary Depth) داشته باشند؟**
   * **در سطح اسکیما بله، در سطح سرویس‌ها و کوئری‌ها محدود به ۲ سطح:** اسکیما با `parentId` بی‌نهایت سطح را پشتیبانی می‌کند، اما توابع جستجوی اکسپلور (`explore.repository.postgres.ts`) فقط فرزندان مستقیم (یک سطح زیرین) را واکشی می‌کنند.
4. **آیا ویژگی‌ها (Attributes) می‌توانند به‌صورت پویا به دسته‌ها متصل شوند؟**
   * **خیر (NOT CURRENTLY IMPLEMENTED):** هیچ جدولی برای `category_attributes`، انواع مقادیر (عددی، انتخابی، گرماژ، نمره نخ و ...) وجود ندارد.
5. **آیا مترادف‌ها و نام‌های مستعار (Synonyms / Aliases) پشتیبانی می‌شوند؟**
   * **خیر (NOT CURRENTLY IMPLEMENTED):** فیلدی برای مترادف‌های اصطلاحات نساجی (مثلاً تریکو = گردباف، راسته دوز = چرخ راسته) وجود ندارد.
6. **آیا تاکسونومی بدون تغییر کد توسعه‌پذیر است؟**
   * **تا حدی:** اضافه کردن رکورد به دیتابیس در API منعکس می‌شود، اما فرانت‌اند بخش تگ‌های افقی را هاردکد نگه‌داشته است.

---

## ۶. ممیزی چرخه حیات آگهی (Listing Lifecycle Audit)

```
[گام‌های واقعی موجود در کد]
کاربر وارد CreateAdModal می‌شود
   ↓
انتخاب قصد تجاری (OFFER یا DEMAND)
   ↓
بررسی سهمیه بسته در کلاینت (entitlementService)
   ↓
ارسال درخواست به POST /api/v1/listings (ایجاد پیش‌نویس draft یا published)
   ↓
افزودن رسانه (POST /api/v1/listings/:id/media)
   ↓
افزودن موقعیت مکانی (POST /api/v1/listings/:id/location)
   ↓
انتشار رسمی (POST /api/v1/listings/:id/publish -> وضعیت published)
   ↓
نمایش در فید عمومی (GET /api/v1/listings) و اکسپلور (GET /api/v1/explore)
   ↓
بایگانی آگهی (POST /api/v1/listings/:id/archive -> وضعیت archived)
   ↓
تعلیق آگهی (POST /api/v1/listings/:id/suspend -> وضعیت suspended)
```

**بررسی انطباق مراحل چرخه حیات:**
* **مراحل موجود و پیاده‌سازی‌شده:** Create Listing, Draft, Validation (Zod), Save, Publish, Display, Explore, Search, Archive, Suspend.
* **مراحل غایب (Missing):**
  * `Expiry` (انقضای خودکار پس از بازه زمانی مشخص مانند ۳۰ روز): **NOT CURRENTLY IMPLEMENTED** (هیچ کرون‌جاب یا فیلد `expiresAt` فعال وجود ندارد).
  * `AI Normalization & Extraction` (استخراج خودکار مشخصات پارچه و متریال از توضیحات): **NOT CURRENTLY IMPLEMENTED**.
* **مفاهیم هم‌پوشان و تکراری (Overlaps):**
  * فیلد `activityType` در جدول `listings` مقادیر `'offer'`, `'need'`, `'capacity'` می‌گیرد، در حالی که در فرانت‌اند `intent` با مقادیر `'OFFER'`, `'DEMAND'` وجود دارد و فیلد `commodityType` نیز مجدداً مقدار `'capacity'` را می‌پذیرد. این تداخل مفهومی بین «قصد مبادله» و «نوع موضوع مبادله» باید در مراحل بعد تفکیک شود.

---

## ۷. ممیزی رابطه شناسنامه، کاربر، بنگاه و آگهی (Profile Architecture Audit)

در سورس‌کد فعلی، تفکیک مسئولیت‌ها به شکل زیر است:

* **USER (`users`):** کنشگر انسانی احراز هویت‌شده دارای شماره موبایل و حساب کاربری.
* **BUSINESS (`businesses` + `business_profiles`):** کارگاه، کارخانه یا دفتر تجاری دارای شماره ثبت، شماره پروانه، آدرس فیزیکی و مساحت کارگاه.
* **MEMBERSHIP (`business_memberships`):** نقش کنشگر در کارگاه (مالک، مدیر، اپراتور).
* **PROFILE (`profiles`):** شناسنامه تجاری B2B فردی یا سازمانی که شامل سوابق، تخصص‌ها، ساعات کاری، شیوه همکاری، مدارک و شعب متعدد است.
* **LISTING (`listings`):** بیانیه و پیشنهاد مقطعی و تجاری عرضه یا تقاضا.

**تحلیل مشکل دوباره‌کاری ورودی از کاربر (Redundant Input Anti-Pattern):**
* **یافته کلیدی ممیزی:** در حال حاضر هنگام ثبت آگهی، سامانه از کاربر می‌خواهد مجدداً `city`، `province`، شماره تماس و آدرس را دستی وارد کند؛ در حالی که این اطلاعات در `profiles` و `businesses` کاربر از قبل با دقت کامل ذخیره شده است.
* آگهی‌های ثبت‌شده در پایگاه داده مستقیماً به `businessId` متصل هستند، اما به جدول جدید `profiles` وصل نشده‌اند. این یک شکاف معماری است که در فاز تکامل باید اصلاح شود تا اطلاعات ثابت به‌صورت خودکار از پروفایل تزریق گردد.

---

## ۸. ممیزی موتور جستجو (Search Audit)

1. **فیلدهای قابل جستجو:**
   * در ریشه سرور (`explore.repository.postgres.ts`): فقط ۳ فیلد متنی:
     * `listings.title`
     * `listings.description`
     * `businesses.name`
2. **نوع کوئری پایگاه داده:**
   * تطبیق رشته‌ای ساده با عملگر `ILIKE '%term%'` در PostgreSQL.
3. **رتبه‌بندی نتایج (Ranking):**
   * صرفاً بر اساس تاریخ درج: `ORDER BY desc(listings.publishedAt), desc(listings.createdAt)`.
   * هیچ الگوریتم رتبه‌بندی متنی (مانند TF-IDF، BM25، پوشش واژگان یا تطبیق عبارتی) وجود ندارد.
4. **فیلتر رسته‌ها و موقعیت:**
   * فیلتر شناسه دسته (`categoryId`) و اسلاگ (`categorySlug`) همراه با بسط یک‌سطحی فرزندان انجام می‌شود.
   * فیلتر استان و شهر با `ILIKE` انجام می‌شود.
5. **جستجوی معنایی / برداری / مبتنی بر هوش مصنوعی (Semantic Search):**
   * **NOT CURRENTLY IMPLEMENTED** (هیچ ایندکس برداری، افزونه `pgvector` یا فرآیند Embedding وجود ندارد).
6. **سیگنال‌های پروفایل در جستجو:**
   * وضعیت تأیید یا امتیاز کارگاه در رتبه‌بندی آگهی‌ها دخالت داده نمی‌شود.

---

## ۹. ممیزی سیستم فیلترها (Filter System Audit)

1. **آیا فیلترها پویا هستند یا ایستا؟**
   * **ایستا (Static):** در فرانت‌اند (`ExploreMarket.tsx`) فیلترهای بالا (تگ‌های افقی مانند طاقه، خرده، مزدی‌دوزی، چرخ، چاپ) یک آرایه ثابت کدنویسی‌شده هستند.
2. **آیا فیلترها متناسب با رسته تغییر می‌کنند (Category-Aware)؟**
   * **خیر (NOT CURRENTLY IMPLEMENTED):** وقتی کاربر وارد رسته «پارچه» می‌شود، فیلترهای مرتبط با پارچه (گرماژ، عرض، جنس نخ، نوع بافت) ظاهر نمی‌شوند؛ فیلترها برای همه دسته‌ها یکسان است.
3. **آیا مقادیر فیلتر از داده‌های ساختاریافته استخراج می‌شوند؟**
   * **خیر:** فیلتر تگ‌ها در فرانت‌اند صرفاً با تابع جاوااسکریپتی `.includes()` روی متن عنوان و توضیحات اجرا می‌شود.

---

## ۱۰. ممیزی لایه هوش مصنوعی (AI Layer Audit)

| شاخص ممیزی هوش مصنوعی | وضعیت در کد فعلی | جزئیات استخراج‌شده از سورس‌کد |
| :--- | :--- | :--- |
| **وظیفه فعلی هوش مصنوعی** | مشاوره چت متنی نساجی | فقط اندپوینت `POST /api/v1/ai/consult` در `server/modules/ai/ai.router.ts`. |
| **ورودی دریافتی (Input)** | متن ساده کاربر (`prompt`) | رشته متنی ارسال‌شده از طریق بدنه درخواست HTTP. |
| **خروجی تولیدی (Output)** | پاسخ متنی تولیدی | متن راهنمایی تخصصی پیرامون گرماژ پارچه، چرخ خیاطی، متن آگهی. |
| **ذخیره‌سازی خروجی در پایگاه داده**| **خیر (NOT IMPLEMENTED)** | پاسخ‌ها صرفاً به مرورگر بازگردانده شده و هیچ رکوردی در DB درج نمی‌شود. |
| **اصالت و منشأ داده (Provenance)** | **خیر (NOT IMPLEMENTED)** | هیچ متادیتایی از توکن‌ها، نسخه پرامپت یا زنجیره استناد ذخیره نمی‌شود. |
| **امتیاز اطمینان (Confidence Score)** | **خیر (NOT IMPLEMENTED)** | میزان اطمینان مدل در خروجی محاسبه و ثبت نمی‌گردد. |
| **تأثیر در جستجو و نمایه‌سازی** | **خیر (NOT IMPLEMENTED)** | هوش مصنوعی هیچ ارتباطی با کوئری‌های سرچ، فید یا ایندکسینگ ندارد. |
| **پردازش تصویر (Multimodal / Vision)**| **خیر (NOT IMPLEMENTED)** | تصاویر پارچه، بافت و ماشین‌آلات به مدل ارسال نمی‌شوند. |
| **پردازش صوت و ویدیو** | **خیر (NOT IMPLEMENTED)** | فایل‌های صوتی یا ویدیوهای کارگاه‌ها پردازش هوشمند نمی‌شوند. |
| **استخراج مشخصات آگهی از متن آزاد** | **خیر (NOT IMPLEMENTED)** | هنوز هیچ پایپ‌لاینی برای استخراج خودکار متریال/گرماژ/ظرفیت از متن آگهی وجود ندارد. |

---

## ۱۱. ممیزی وضعیت تست‌ها (Current Tests Audit)

تست‌های سرور در پوشه `server/tests/` قرار دارند و توسط فرمان `npm run test` ارزیابی می‌شوند:

* **تست‌های موجود:**
  1. `vertical-slice.test.ts`: راستی‌آزمایی ماندگاری پس از ری‌استارت، گارد محافظتی عدم دسترسی متقابل بین کارگاه‌ها (Cross-Business Authorization Guard) و جلوگیری از تغییر وضعیت‌های غیرمجاز چرخه حیات (State Transition Guard).
  2. `vertical-slice-02.test.ts`: اعتبارسنجی جریان داده از هویت کاربر تا ساخت کارگاه، ثبت پیش‌نویس، اتصال رسانه و انتشار.
  3. `vertical-slice-02-api.test.ts`: تست کامل ۱۶ اندپوینت HTTP با ایجاد سرور واقعی موقت Express روی پورت تصادفی.
  4. `vertical-slice-03.test.ts`: تست یکپارچگی دسته‌بندی‌ها، درخت تاکسونومی، اندپوینت‌های اکسپلور و فیلتر شهرها.
  5. `vertical-slice-profile.test.ts`: تست ماژول شناسنامه پایدار B2B (ثبت فرد/سازمان، اعتبارسنجی فیلدهای ضروری، ثبت امتیازات).
* **آنچه تحت پوشش نیست (Missing Test Coverage):**
  * هیچ تست کلاینتی فرانت‌اند (Unit / Component Test با Vitest یا Playwright) وجود ندارد.
  * تست‌های بار و هم‌زمانی تراکنش‌های دیتابیس (Concurrency / Race Conditions) وجود ندارد.
  * سناریوهای خرابی شبکه دیتابیس و اعتبارسنجی تایم‌اوت هوش مصنوعی تست نشده است.

---

## ۱۲. جدول ماتریس آمادگی برای پروداکشن (Production Readiness Matrix)

| حیطه مهندسی و عملیاتی | وضعیت | ارزیابی واقع‌بینانه فنی بر اساس سورس‌کد |
| :--- | :---: | :--- |
| **امنیت عمومی (Security)** | **PARTIAL** | میدلورهای استاندارد اکسپرس فعال هستند، اما CSP و هدرهای امنیتی Helmet نصب نشده است. |
| **احراز هویت (Authentication)** | **PARTIAL** | ساختار کانتکست و Bearer توکن دارد، اما امضای کریپتوگرافیک توکن‌ها در بک‌اند اعتبارسنجی نمی‌شود. |
| **کنترل دسترسی (Authorization)**| **CURRENT** | مدل مالکیت کارگاه و عضویت در تغییر آگهی‌ها با خطای ۴۰۳/۴۰۴ با دقت محافظت شده است. |
| **اعتبارسنجی ورودی‌ها (Validation)**| **CURRENT** | اسکیمای Zod در تمامی روت‌های POST و PATCH ورودی‌ها را قبل از ورود به DB فیلتر می‌کند. |
| **کنترل نرخ درخواست (Rate Limiting)**| **CURRENT** | پکیج `express-rate-limit` برای روت‌های عمومی و به شکل اختصاصی‌تر برای اندپوینت‌های هوش مصنوعی فعال است. |
| **یکپارچگی دیتابیس (DB Integrity)** | **CURRENT** | کلیدهای خارجی، قیدهای یکتایی اسلاگ‌ها و Cascade Delete در Drizzle تعریف و اعمال شده‌اند. |
| **مدیریت خطاها (Error Handling)** | **CURRENT** | ساختار انولوپ استاندارد `res.apiError` و کدهای خطای دامنه‌ای منسجم تعریف شده است. |
| **ثبت وقایع (Logging)** | **PARTIAL** | از `console.log` و `console.error` استاندارد استفاده شده؛ لاگر ساختاریافته (مانند Pino/Winston) وجود ندارد. |
| **پایش و سلامت (Monitoring)** | **PARTIAL** | روت اختصاصی `/api/v1/health` برای پروب‌های Liveness و Readiness سرور ایجاد شده است. |
| **ذخیره‌سازی فایل (File Storage)** | **PARTIAL** | نگاشت رسانه در جدول `media` ساختارمند است، اما آپلود مستقیم باینری به باکت ابری (S3/GCS) هنوز متصل نیست. |
| **پردازش رسانه (Media Processing)**| **MISSING** | تغییر اندازه تصاویر، تبدیل خودکار به فرمت مدرن WebP یا فشرده‌سازی در سرور غایب است. |
| **مقیاس‌پذیری جستجو** | **PARTIAL** | کوئری‌های `ILIKE` برای دیتای کم‌حجم سریع هستند اما برای میلیون‌ها رکورد نیازمند ایندکس Full-Text یا موتور اختصاصی است. |
| **حافظه نهان (Caching)** | **MISSING** | ریدیس (Redis) یا این‌مموری کشینگ برای نتایج فید، درخت دسته‌بندی و اطلاعات پروفایل وجود ندارد. |
| **صف و کارهای پس‌زمینه (Queues)** | **MISSING** | سیستم صف (BullMQ / Celery) برای پردازش‌های پس‌زمینه (استخراج هوش مصنوعی، ارسال پیامک) وجود ندارد. |
| **کنترل هزینه AI (Cost Control)** | **PARTIAL** | محدودیت Rate Limiter برای روت AI تعریف شده، اما سقف توکن روزانه برای هر کاربر لاگین‌شده وجود ندارد. |
| **مدیریت محرمانه‌ها (Secrets)** | **CURRENT** | متغیرهای محیطی از طریق فایل‌های `.env` و متغیرهای استاندارد بارگذاری می‌شوند. |
| **پشتیبان‌گیری (Backup / Recovery)**| **UNKNOWN** | بستگی به سیاست‌های سرویس‌دهنده ابری دیتابیس PostgreSQL دارد و در کدهای برنامه تعریف نشده است. |
| **پوشش تست‌ها (Testing)** | **CURRENT** | ۵ سوئیت تست جامع یکپارچگی روی جریان‌های اصلی تجاری سرور و دیتابیس پاس می‌شوند. |

---

## ۱۳. نگاشت به معماری هدف تاروپود (Target Architecture Gap Mapping)

| مفهوم در معماری آرمانی تاروپود | وضعیت پیاده‌سازی فعلی در مخزن | شکاف ساختاری موجود (The Gap) | اقدام معماری پیشنهادی در مراحل بعد |
| :--- | :--- | :--- | :--- |
| **ACTOR (کنشگر)** | پیاده‌سازی دوگانه در `users` و `businesses` | تفکیک شفاف بین کاربر حقیقی و ماهیت حقوقی کنشگر برقرار نیست. | تعریف لایه یکپارچه Actor با زیرمجموعه‌های Person و Business. |
| **PROFILE (شناسنامه پایدار)**| جدول مجزای `profiles` با نوع Person/Org | آگهی‌های جاری به جدول `profiles` لینک نشده‌اند (به `businesses` لینک هستند). | ایجاد کلید ارتباطی تمیز بین شناسنامه و آگهی‌های منتشرشده. |
| **MARKET OBJECT (موضوع مبادله)**| فیلد ساده `commodity_type` در آگهی | مفاهیم متریال، تجهیزات، ظرفیت و پروژه همگی در یک جدول متنی آگهی ساده فشرده شده‌اند. | طراحی لایه تایپ‌های تجاری تخصصی با ویژگی‌های فنی خاص خود. |
| **ROLE (نقش‌های هم‌زمان کنشگر)**| فیلد متنی `workGroup` و `specialties` | کنشگر نمی‌تواند هم‌زمان نقش خریدار عمده و تولیدکننده را به‌صورت مدولار سوییچ کند. | تعریف ساختار چندنقشی مستقل برای هر پروفایل. |
| **INTENT (قصد تجاری)** | فیلد `intent` در فرانت‌اند و `activity_type` در سرور| تفکیک مفهومی در فرانت‌اند آغاز شده اما در سرور بین قصد و نوع کالا ادغام است. | تثبیت شفاف Intent (عرضه OFFER در برابر تقاضا DEMAND). |
| **CLASSIFICATION (طبقه‌بندی)**| جدول سلسله‌مراتبی `categories` | طبقه‌بندی صرفاً تا ۲ سطح انجام شده و ویژگی‌های استاندارد فنی ندارد. | ارتقای درخت دسته‌ها با پشتیبانی از عمق دلخواه و برچسب‌های صنعتی. |
| **ATTRIBUTE (ویژگی‌های فنی)**| **NOT CURRENTLY IMPLEMENTED** | ویژگی‌هایی نظیر گرماژ (GSM)، نمره نخ، نوع بافت، برق مصرفی و ... وجود ندارد. | ایجاد مدل داده EAV یا فیلدهای ساختاریافته JSONB با ایندکس GIN. |
| **STATE (وضعیت و چرخه حیات)**| فیلد `status` در `listings` (draft, published, etc.)| تغییر وضعیت‌ها در دیتابیس وجود دارد اما تاریخ انقضا و بازبینی ندارد. | افزودن ماشین وضعیت رسمی (State Machine) همراه با انقضای خودکار. |
| **LOCATION (موقعیت و پهنه)**| جداول `locations`، `listing_locations`، `profile_locations`| مدل داده موقعیت عالی است، اما جستجوی شعاعی و بر مبنای فاصله جغرافیایی فعال نیست. | فعال‌سازی جستجوی مبتنی بر فاصله جغرافیایی (GIS). |
| **MEDIA (رسانه و تصویر)** | جدول `media` و پیوندهای رابطه‌ای | ذخیره انجام می‌شود، اما پردازش اندازه و بهینه‌سازی رسانه انجام نمی‌شود. | اتصال به پایپ‌لاین فشرده‌سازی و تولید ابعاد مختلف رسانه. |
| **TRUST / CREDENTIAL (اعتبار)**| جدول `profile_credentials` و فیلدهای امتیاز | مدارک ثبت می‌شوند اما فرآیند تأیید سیستمی مدارک نیازمند پنل ناظر است. | توسعه سطوح اعتبار و نشان‌های رسمی اتحادیه‌های صنفی. |
| **RELATIONSHIP (روابط تجاری)** | جدول `business_memberships` و `profile_ratings` | شبکه ارتباطات صنفی میان کارگاه‌ها (مانند پیمانکار-کارفرما) مدل نشده است. | تعریف روابط شبکه‌ای میان شناسنامه‌های تجاری مختلف. |
| **PROVENANCE (منشأ اطلاعات)** | فیلدهای `createdById` و `profile_audit_events` | اصالت ثبت کاربر عالی است اما منشأ استخراج هوش مصنوعی ثبت نمی‌شود. | ثبت فراداده منشأ اطلاعات (ورودی مستقیم کاربر یا استخراج مدل هوش مصنوعی). |
| **AI / DERIVED DATA (داده مشتق)**| **NOT CURRENTLY IMPLEMENTED** | هیچ فیلد یا برداری برای مشخصات استخراج‌شده خودکار وجود ندارد. | طراحی جدول داده‌های مشتق هوش مصنوعی با شاخص اطمینان (Confidence). |
| **BEHAVIOR / EVENTS (رفتار)** | فقط شمارنده ساده `viewsCount` | کلیک روی تماس، چت، ذخیره آگهی و سوابق جستجو لاگ ساختاریافته ندارند. | ایجاد رویدادهای تحلیلی برای یادگیری رفتاری بازارگاه. |

---

## ۱۴. ریسک‌های مهاجرت معماری (Migration Risks)

تغییر شتاب‌زده یا ناگهانی ساختارهای زیر در این مقطع زمانی **بسیار پرخطر** است و می‌تواند موجب از کار افتادن کل سامانه گردد:

1. **ریسک تغییر جدول `listings`:**  
   بخش‌های زیادی از فرانت‌اند (`ExploreMarket.tsx`، `HomeFeed.tsx`، `AdDetailModal.tsx`) مستقیماً به فیلدهای فعلی این جدول متصل هستند. هرگونه تغییر نام یا حذف فیلد باید از طریق گسترش تدریجی (Extend) و سازگاری عقبروی انجام شود.
2. **ریسک شکستن پیوند کارگاه‌ها و آگهی‌ها (`businessId` در `listings`):**  
   حذف یا بازنویسی ناگهانی جدول `businesses` موجب شکستن کلیدهای خارجی تمام آگهی‌های موجود خواهد شد.
3. **ریسک تاکسونومی و اسلاگ دسته‌ها (`categorySlug`):**  
   فید اصلی و فرانت‌اند برای فیلتر سریع از اسلاگ‌های متنی استفاده می‌کنند. تغییر فرمت اسلاگ‌ها موجب برگرداندن صفحه خالی به کاربر می‌شود.
4. **ریسک تضاد بین `profiles` و `businesses`:**  
   در حال حاضر بخشی از برنامه اطلاعات کارگاه را از روت‌های جدید `profiles` می‌خواند و در صورت عدم وجود به `businesses` رجوع می‌کند. هرگونه مهاجرت باید این Fallback را تا تکمیل ادغام حفظ کند.
5. **ریسک قرادادهای API (`/api/v1/*`):**  
   اندپوینت‌های فعلی پاسخ‌ها را در انولوپ `{ success, data, error, meta }` بازمی‌گردانند. تغییر ساختار پاسخ‌ها کلاینت را دچار خطای رندرینگ می‌کند.

---

## ۱۵. برنامه زمان‌بندی و نقشه راه پیشنهادی پیاده‌سازی (Staged Implementation Roadmap)
*(طراحی‌شده در حداکثر ۸ مرحله منطقی و بدون شکستن عملکردهای جاری)*

```
[مرحله ۱: یکسان‌سازی کاتالوگ طبقه‌بندی و تاکسونومی]
   ↓
[مرحله ۲: تثبیت مدل هویتی کنشگر (Actor/Profile Bridge)]
   ↓
[مرحله ۳: معماری ویژگی‌های فنی پویا (Dynamic Attributes Layer)]
   ↓
[مرحله ۴: تفکیک قصد (Intent) و گسترش موجودیت‌های تجاری (Market Objects)]
   ↓
[مرحله ۵: پایپ‌لاین هوش مصنوعی برای استخراج و غنی‌سازی خودکار داده‌ها]
   ↓
[مرحله ۶: ارتقای موتور جستجو، ایندکسینگ و فیلترهای چندبعدی متناسب با رسته]
   ↓
[مرحله ۷: یکپارچه‌سازی فرانت‌اند بدون دوباره‌کاری ورودی از کاربر]
   ↓
[مرحله ۸: سخت‌سازی امنیتی، پایش و آمادگی کامل پروداکشن]
```

### شرح تفصیلی مراحل نقشه راه:

#### مرحله ۱: یکسان‌سازی کاتالوگ طبقه‌بندی و تاکسونومی (Taxonomy Normalization)
* **هدف:** حذف تگ‌های هاردکدشده از کلاینت و تکمیل ساختار دسته‌بندی در دیتابیس با پشتیبانی از عمق دلخواه و مترادف‌ها.
* **فایل‌های موجود متأثر:** `server/modules/taxonomy/*`, `src/components/ExploreMarket.tsx`.
* **فایل‌های جدید:** `server/modules/taxonomy/taxonomy.aliases.ts`.
* **تأثیر دیتابیس:** افزودن جدول `category_aliases` (مترادف‌های اصطلاحات نساجی).
* **سطح ریسک:** پایین (Low).
* **پیش‌نیاز:** ندارد.
* **آنچه نباید تغییر کند:** جدول `listings` و کلیدهای خارجی فعلی.

#### مرحله ۲: تثبیت پل ارتباطی مدل هویتی کنشگر (Actor & Profile Bridge)
* **هدف:** متصل کردن یکپارچه `businesses` به `profiles` بدون تخریب کدهای قبلی تا هر بنگاه دارای شناسنامه متناظر باشد.
* **فایل‌های موجود متأثر:** `server/modules/profiles/*`, `server/modules/businesses/*`, `src/components/ProfileView.tsx`.
* **تأثیر دیتابیس:** اضافه شدن ستون اختیاری `profileId` به جدول `businesses` و مایگریشن امن دیتای قدیمی.
* **سطح ریسک:** متوسط (Medium).
* **پیش‌نیاز:** مرحله ۱.
* **آنچه نباید تغییر کند:** جدول `users` و سازوکار احراز هویت.

#### مرحله ۳: لایه ویژگی‌های فنی پویا (Dynamic Technical Attributes Layer)
* **هدف:** امکان تعریف ویژگی‌های فنی استاندارد نساجی (گرماژ، نمره نخ، نوع ماشین دوخت، عرض پارچه) متصل به هر رسته.
* **فایل‌های جدید:** `server/modules/taxonomy/attributes.*`، جداول جدید `category_attributes` و `listing_attribute_values`.
* **تأثیر دیتابیس:** ایجاد جداول ساختاریافته برای ثبت ویژگی‌های کمی و کیفی.
* **سطح ریسک:** پایین (Low).
* **پیش‌نیاز:** مرحله ۱.
* **آنچه نباید تغییر کند:** ستون‌های اصلی `listings.title` و `listings.description`.

#### مرحله ۴: تفکیک رسمی قصد تجاری و گونه‌های مبادله (Intent & Market Objects)
* **هدف:** جداسازی شفاف بین «قصد» (OFFER / DEMAND) و «گونه موضوع مبادله» (محصول، مواد، تجهیزات، خدمت، ظرفیت، شغل).
* **فایل‌های موجود متأثر:** `server/modules/listings/*`, `src/components/CreateAdModal.tsx`, `src/types.ts`.
* **تأثیر دیتابیس:** استانداردسازی مقادیر `activity_type` و `commodity_type` با حفظ مقادیر قبلی.
* **سطح ریسک:** متوسط (Medium).
* **پیش‌نیاز:** مرحله ۳.
* **آنچه نباید تغییر کند:** چرخه حیات پیش‌نویس تا انتشار.

#### مرحله ۵: پایپ‌لاین هوش مصنوعی برای استخراج داده از متن آزاد (AI Ingestion & Normalization)
* **هدف:** دریافت توضیحات کوتاه کاربر و استخراج خودکار متغیرهای فنی، رسته و کلیدواژه‌ها با ذخیره اصالت و ضریب اطمینان.
* **فایل‌های موجود متأثر:** `server/modules/ai/*`, `server/modules/listings/listings.service.ts`.
* **فایل‌های جدید:** `server/modules/ai/ai.extractor.service.ts` و جدول `listing_ai_extractions`.
* **تأثیر دیتابیس:** اضافه شدن جدول ثبت استخراج‌های هوش مصنوعی (AI Derived Data).
* **سطح ریسک:** متوسط (Medium).
* **پیش‌نیاز:** مرحله ۳ و ۴.
* **آنچه نباید تغییر کند:** متن اصلی نوشته‌شده توسط کاربر.

#### مرحله ۶: ارتقای موتور جستجو، ایندکسینگ و فیلترهای حساس به رسته (Category-Aware Filters & Search)
* **هدف:** جایگزینی جستجوی `ILIKE` ساده با جستجوی مبتنی بر متن کامل PostgreSQL و فعال‌سازی فیلترهای پویا در اکسپلور.
* **فایل‌های موجود متأثر:** `server/modules/explore/*`, `src/components/ExploreMarket.tsx`.
* **تأثیر دیتابیس:** افزودن ایندکس‌های Full-Text Search (GIN) روی عنوان، مشخصات و نام کارگاه.
* **سطح ریسک:** متوسط (Medium).
* **پیش‌نیاز:** مرحله ۵.
* **آنچه نباید تغییر کند:** قرارداد پاسخ اندپوینت `/api/v1/explore`.

#### مرحله ۷: حذف دوباره‌کاری در فرانت‌اند و تزریق داده از شناسنامه (Zero-Redundancy Frontend)
* **هدف:** هنگام ثبت آگهی، شهر، استان، شماره تماس، آدرس و نام کارگاه به‌صورت خودکار از پروفایل بارگذاری شود.
* **فایل‌های موجود متأثر:** `src/components/CreateAdModal.tsx`, `src/components/HomeFeed.tsx`.
* **سطح ریسک:** پایین (Low).
* **پیش‌نیاز:** مرحله ۲ و ۴.
* **آنچه نباید تغییر کند:** طراحی کلی و سیستم کالیته ۵ رنگ برندینگ.

#### مرحله ۸: سخت‌سازی امنیتی، پایش و بسته‌بندی پروداکشن (Production Hardening)
* **هدف:** اعتبارسنجی امنیتی توکن‌ها، راه‌اندازی لاگینگ ساختاریافته، اتصال به ذخیره‌ساز ابری باینری و بهینه‌سازی نهایی.
* **فایل‌های متأثر:** `server/middleware/auth.ts`, `server.ts`.
* **سطح ریسک:** پایین (Low).
* **پیش‌نیاز:** مراحل ۱ تا ۷.

---

## جمع‌بندی نهایی بخش‌های ممیزی (Summary Sections)

### A. خلاصه وضعیت کنونی (Current State Summary)
پلتفرم تاروپود در حال حاضر دارای یک اسکلت فنی مستحکم، مدرن و باکیفیت است: معماری ماژولار مونولیت در بک‌اند، اتصال پایگاه داده PostgreSQL با Drizzle ORM، تست‌های یکپارچگی موفق، رابط کاربری روان ری‌اکت ۱۹ با سیستم کالیته ۵ رنگ برندینگ صنعتی. با این وجود، تمرکز فعلی بیشتر بر چرخه حیات پایه‌ای آگهی بوده و هنوز قابلیت‌های متمایز B2B نظیر استخراج خودکار هوش مصنوعی، ویژگی‌های فنی پویا، جستجوی هوشمند متناسب با رسته و تفکیک موجودیت‌های پیچیده صنعتی به‌صورت عملیاتی در دیتابیس فعال نشده‌اند.

### B. نقشه معماری (Architecture Map)
* **فرانت‌اند:** React 19 + Tailwind v4 + Vite + ماژول کلاینت `entitlementService`.
* **بک‌اند:** Node.js Express Modular Monolith با ۷ دامنه مجزا (`profiles`, `listings`, `businesses`, `locations`, `taxonomy`, `explore`, `ai`).
* **داده‌ها:** PostgreSQL با ۱۸ جدول پوشش‌دهنده کاربران، کارگاه‌ها، پروفایل‌ها، آگهی‌ها، رسانه، موقعیت‌ها و بسته‌های تجاری.

### C. نقشه دیتابیس (Database Map)
پایگاه داده در وضعیت **کاملاً نرمال‌شده و رابطه‌ای** قرار دارد. هیچ کوئری مخربی دیده نمی‌شود. دوگانگی موجود بین جدول اولیه `businesses` و جدول پیشرفته `profiles` مهم‌ترین بخش نیازمند ادغام در فازهای بعدی است.

### D. نقشه مدل دامنه (Domain Model Map)
مفاهیم کنشگر (Actor)، آگهی (Listing)، مکان (Location)، چندرسانه‌ای (Media) و رسته (Taxonomy) پیاده‌سازی شده‌اند. مفاهیم کاتالوگ ویژگی‌های پویا (Attributes)، استخراج هوش مصنوعی (AI Derived Data)، کالاهای دیجیتال (Digital Products) و فرصت‌های شغلی ساختاریافته هنوز غایب هستند.

### E. ممیزی تاکسونومی (Taxonomy Audit)
ساختار سلسله‌مراتبی در دیتابیس وجود دارد، اما عملاً در دو سطح به همراه یک فایل سید اجرا می‌شود. فیلترهای نوار افقی فرانت‌اند به دیتابیس وصل نیستند و ویژگی‌های فنی متصل به دسته‌ها هنوز پیاده‌سازی نشده‌اند.

### F. ممیزی جستجو و فیلتر (Search/Filter Audit)
جستجوی فعلی یک جستجوی رشته‌ای متنی ساده با عملگر `ILIKE` در دیتابیس است. هیچ رتبه‌بندی کیفی یا وزن‌دهی مبتنی بر هوش مصنوعی وجود ندارد. فیلترها ایستا بوده و متناسب با نوع رسته تغییر نمی‌کنند.

### G. ممیزی هوش مصنوعی (AI Audit)
هوش مصنوعی فعلی در نقش یک چت‌بات مشاور متنی عمل می‌کند و پاسخ‌ها ذخیره نمی‌شوند. هیچ ارتباطی میان هوش مصنوعی و پایپ‌لاین ثبت، استخراج مشخصات، دسته‌بندی خودکار یا ایندکسینگ جستجو وجود ندارد.

### H. ممیزی آمادگی پروداکشن (Production Readiness Audit)
بخش‌های اعتبارسنجی ورودی (Zod)، کنترل دسترسی کارگاه‌ها، یکپارچگی ارجاعی دیتابیس و مدیریت خطاها در وضعیت مطلوب (**CURRENT**) هستند. بخش‌های احراز هویت توکن، کشینگ، صف‌های پس‌زمینه و پردازش بهینه تصاویر در وضعیت ناکامل یا غایب (**PARTIAL / MISSING**) قرار دارند.

### I. نقشه شکاف با معماری آرمانی (Target Architecture Gap Map)
شکاف‌های اصلی شامل: نبود ویژگی‌های فنی داینامیک، عدم اتصال شناسنامه به آگهی، عدم استخراج مشخصات با AI، و نبود ایندکس معنایی برای تطبیق دقیق عرضه و تقاضا در صنعت نساجی است.

### J. ۱۰ ریسک برتر معماری (Top 10 Architectural Risks)
1. شکستن کلیدهای خارجی آگهی‌ها در صورت دستکاری جدول `businesses`.
2. ایجاد خطا در فرانت‌اند در صورت تغییر ساختار انولوپ پاسخ‌های `/api/v1/*`.
3. از کار افتادن فید در صورت تغییر فرمت `categorySlug` بدون سازگاری عقبروی.
4. کندی کوئری‌های `ILIKE` با افزایش تعداد آگهی‌ها به بیش از چند هزار رکورد.
5. بارگذاری کند تصاویر به دلیل عدم تغییر سایز و عدم فشرده‌سازی در سرور.
6. هزینه‌های بالای هوش مصنوعی در صورت عدم ذخیره‌سازی نتایج استخراج‌شده.
7. دوباره‌کاری و خستگی کاربر هنگام ثبت آگهی به دلیل عدم استفاده از دیتای پروفایل.
8. عدم امکان جستجوی کالاهای نساجی بر اساس مشخصات فنی (گرماژ و نمره نخ).
9. سردرگمی کاربر میان فیلدهای هم‌پوشان `activityType` و `commodityType`.
10. عدم انقضای خودکار آگهی‌های قدیمی و کهنه شدن محتوای بازارگاه.

### K. نقشه راه ۸ مرحله‌ای پیاده‌سازی (8-Stage Roadmap)
مراحل ۸ گانه به‌صورت دقیق و متوالی از یکسان‌سازی تاکسونومی، ایجاد پل پروفایل-بنگاه، لایه ویژگی‌های پویا، تفکیک قصد، پایپ‌لاین استخراج AI، ارتقای موتور جستجو، اصلاح فرانت‌اند تا سخت‌سازی نهایی پروداکشن تعریف شدند.

### L. فایل‌های دقیق برای فاز اول پیاده‌سازی (Stage 1 Files)
در **مرحله اول**، صرفاً فایل‌های زیر باید هدف قرار گیرند:
1. `server/modules/taxonomy/taxonomy.seeds.ts` (غنی‌سازی رسته‌ها و افزودن متادیتای مترادف‌ها)
2. `server/modules/taxonomy/taxonomy.repository.postgres.ts` (بهبود کوئری‌های واکشی فرزندان درخت)
3. `server/modules/taxonomy/taxonomy.service.ts` (پشتیبانی از واکشی با مترادف)
4. `src/core/api/taxonomy.service.ts` (انتقال تایپ‌های به‌روزشده به فرانت‌اند)
5. `src/components/ExploreMarket.tsx` (اتصال تگ‌های افقی نوار ناوبری به داده‌های واقعی دیتابیس به جای آرایه هاردکدشده)

---
*پایان سند ممیزی معماری تاروپود - کلیه کدهای اجرایی موجود در این مرحله بدون هیچ‌گونه دستکاری یا تغییر باقی ماندند.*
